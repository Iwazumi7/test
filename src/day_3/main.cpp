#include <stdio.h>
#include <string>
#include <array>


constexpr int DIM = 3;

int main(){
    std::array<int, DIM> arr = {0,3,5};
    double dct_coeff;
    double *obj_ptr;
    std::array<double, DIM> normal = {0.0, 1.0, 0.0};

    return 0;
}